cloud:
  aws:
    credentials:
      access-key: xxxxx
      secret-key: xxxxx
    region:
      static: us-east-1
    stack:
      auto: false

application:
  bucket:
    name: hanu-banking-system-storage

  jwtSecret: HANUBankingSystem
  jwtExpirationMs: 86400000


spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB
